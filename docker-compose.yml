version: "3.5"

services:
  frontend:
    image: frontend
    build: 
      context: .
      dockerfile: Dockerfile
      args: 
        - REACT_APP_BACKEND_URL=http://192.168.0.117:5000

    ports:
      - "80:80"
    container_name: reading-list-frontend

  # reverse_proxy:
  #   image: nginx:1.19-alpine
  #   restart: unless-stopped
  #   volumes:
  #     - ./nginx.conf:/etc/nginx/nginx.conf
  #   ports:
  #     - 80:80
  #   environment:
  #     - NGINX_PORT=80
